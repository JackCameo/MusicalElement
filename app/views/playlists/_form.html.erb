<%= form_for(@playlist, :url => library_playlists_path(@library) , :html => {:multipart => true, :remote => :true}) do |f| %>

  <% if @playlist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@playlist.errors.count, "error") %> prohibited this playlist from being saved:</h2>

      <ul>
      <% @playlist.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <% f.fields_for @tracks do |track| %>
    <%= track.check_box :tracks %>
    <%= track.label :tracks, "Add Song" %>
    <% end %>

<!--   <div class="field">
    <%#= f.label :name %><br />
    <%#= f.text_field :name %>
  </div> -->
<!--   <div class="field">
      <%#= f.hidden_field :library_id %>
  </div> -->

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%#= f.hidden_field :library_id %>
  </div>

  <div class="field">
    <%#= f.hidden_field :user_id %>
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

:position, :track_id, :user_id, :library_id